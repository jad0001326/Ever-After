import { PlaceholderScreen } from "../../src/ui/PlaceholderScreen";

export default function SignInScreen() {
  return <PlaceholderScreen title="Welcome back" body="Secure sign-in is prepared for N3 and is not connected in this local prototype." />;
}
